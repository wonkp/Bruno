info:
  name: 评测分页
  type: http
  seq: 16

http:
  method: POST
  url: "{{teaching-url}}/studentWorkbookRel/page"
  body:
    type: json
    data: |-
      {
        "pageSize": 10,
        "page": 1,
        "type": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
