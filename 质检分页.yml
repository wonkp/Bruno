info:
  name: 质检分页
  type: http
  seq: 13

http:
  method: POST
  url: "{{teaching-url}}/quality/page"
  body:
    type: json
    data: |-
      {
        "pageSize": 10,
        "page": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
