info:
  name: 获得预览地址
  type: http
  seq: 16

http:
  method: GET
  url: "{{system-url}}/file/preview?id=102"
  params:
    - name: id
      value: "102"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
