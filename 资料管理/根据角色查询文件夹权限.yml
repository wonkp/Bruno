info:
  name: 根据角色查询文件夹权限
  type: http
  seq: 16

http:
  method: GET
  url: "{{system-url}}/file/queryFileIdsByRoleId?roleId=100"
  params:
    - name: roleId
      value: "100"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
