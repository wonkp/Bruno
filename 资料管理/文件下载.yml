info:
  name: 文件下载
  type: http
  seq: 16

http:
  method: GET
  url: "{{teaching-url}}/courseMaterial/download?id=17738"
  params:
    - name: id
      value: "17738"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
