info:
  name: 查询文件（懒加载）
  type: http
  seq: 16

http:
  method: POST
  url: "{{system-url}}/file/dir/tree/permission"
  body:
    type: json
    data: |
      {
        "parentId":null,
        "isDir":1,
        "keyword":null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
