info:
  name: 删除文件
  type: http
  seq: 16

http:
  method: DELETE
  url: "{{system-url}}/file"
  body:
    type: json
    data: |
      [
          70
        ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
