info:
  name: 创建文件夹
  type: http
  seq: 16

http:
  method: POST
  url: "{{system-url}}/file/directory"
  body:
    type: json
    data: |-
      {
        "folderName":"1",
        "parentId":70
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
