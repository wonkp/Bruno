info:
  name: 质检导出
  type: http
  seq: 13

http:
  method: POST
  url: "{{teaching-url}}/quality/export"
  body:
    type: json
    data: |-
      {
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
