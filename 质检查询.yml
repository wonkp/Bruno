info:
  name: 质检查询
  type: http
  seq: 12

http:
  method: GET
  url: "{{teaching-url}}/quality/id?clazzId=1"
  params:
    - name: clazzId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
