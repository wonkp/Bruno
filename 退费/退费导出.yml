info:
  name: 退费导出
  type: http
  seq: 25

http:
  method: POST
  url: "{{sale-url}}/refund/export"
  body:
    type: json
    data: |-
      {
          "channelCodes": [],
          "refundId": 805
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
