info:
  name: 退费分页
  type: http
  seq: 24

http:
  method: POST
  url: "{{sale-url}}/refund/page"
  body:
    type: json
    data: |-
      {
          "accountMonths": ["2024-06"]
          "ccIds":[14,15,16]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
