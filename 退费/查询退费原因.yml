info:
  name: 查询退费原因
  type: http
  seq: 24

http:
  method: GET
  url: "{{sale-url}}/refund/refund/reason"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
