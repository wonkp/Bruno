info:
  name: 触发分析流程
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8083/teaching-web/playback/agent?clazzId=1
  params:
    - name: clazzId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
