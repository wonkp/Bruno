info:
  name: 触发分析流程
  type: http
  seq: 2

http:
  method: GET
  url: "{{teaching-url}}/playback/agent?clazzId=1"
  params:
    - name: clazzId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
