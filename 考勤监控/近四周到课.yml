info:
  name: 近四周到课
  type: http
  seq: 19

http:
  method: GET
  url: "{{teaching-url}}/attendance/clazz/stat/374434"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
