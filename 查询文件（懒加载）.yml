info:
  name: 查询文件（懒加载）
  type: http
  seq: 21

http:
  method: POST
  url: "{{system-url}}/file/list"
  body:
    type: json
    data: |
      {
        "parentId":null,
        "isDir":null,
        "keyword":null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
